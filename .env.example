# Study Flow uses FREE AI only (Ollama + optional Gemini).
# No API keys are needed in the frontend.
# For the backend (server/), see server/.env.example and SETUP.md.

# Optional: bootstrap first admin. Set to your email; app will set your user doc to role=admin, status=approved on sign-in.
# See ADMIN_SETUP.md. Remove after first admin is created.
# VITE_ADMIN_EMAIL=your-email@example.com
